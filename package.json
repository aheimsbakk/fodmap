{
  "name": "fodmap-overview",
  "version": "1.0.0",
  "private": true,
  "description": "Static reimplementation of the FODMAP food overview page",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "format": "prettier --write \"src/**/*.{html,css,js}\" \"tests/**/*.js\"",
    "verify:sync": "bash scripts/verify_codebase_sync.sh"
  },
  "devDependencies": {
    "jsdom": "29.1.1",
    "prettier": "3.9.6"
  }
}
