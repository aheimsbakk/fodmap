{
  "name": "fodmap-overview",
  "version": "1.0.0",
  "private": true,
  "description": "Static, dependency-free single-page FODMAP food overview",
  "type": "module",
  "scripts": {
    "build": "bash scripts/build.sh",
    "test": "node --test \"tests/*.test.js\"",
    "format": "prettier --write \"src/**/*.{html,css,js}\" \"tests/**/*.js\"",
    "verify:sync": "bash scripts/verify_codebase_sync.sh"
  },
  "devDependencies": {
    "jsdom": "29.1.1",
    "prettier": "3.9.6"
  }
}
